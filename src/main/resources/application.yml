spring:
  application:
    name: GlobalPulse
  
  datasource:
    url: jdbc:mysql://localhost:3306/globalpulse?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Raja@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970 # 256-bit hex secret
  expiration: 86400000 # 24 hours in ms

# Logging
logging:
  level:
    org.springframework.web: INFO
    com.globalpulse: DEBUG
    org.hibernate.SQL: DEBUG
